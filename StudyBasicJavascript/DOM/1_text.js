//DOM (Document object model)

/** Gồm 3 thành phần chính:
 * Element (các thẻ,..)
 * Atribute (thuộc tính của các thẻ: title, style, href,..)
 * Text: văn bản
 */

//Node: là điểm giao nhau

/*
    HTML DOM là viết tắt của Document Object Model trong HTML.
    Nó là một giao diện lập trình ứng dụng (API) cho HTML và XML,
    cho phép các ngôn ngữ lập trình như JavaScript tương tác và thao tác với các phần tử HTML trên trang web.
    Khi một trang web được tải vào trình duyệt, trình duyệt sẽ phân tích cú pháp (parse) các mã HTML 
    và xây dựng một cấu trúc cây được gọi là DOM. DOM bao gồm tất cả các phần tử HTML của trang, 
    được tổ chức thành một cây phân cấp, trong đó mỗi phần tử là một nút trong cây.
    Nhờ vào DOM, bạn có thể sử dụng JavaScript để truy cập, 
    thay đổi và tương tác với các phần tử HTML trên trang web một cách động. 
    Bằng cách sử dụng DOM, bạn có thể thêm hoặc xóa các phần tử, thay đổi nội dung, 
    lấy giá trị của các trường nhập, thay đổi kiểu và lớp của phần tử, 
    và thực hiện nhiều hoạt động khác liên quan đến HTML.
    DOM cũng cho phép bạn lắng nghe và xử lý các sự kiện như nhấp chuột,
    nhập liệu và thay đổi trạng thái của trang. Điều này giúp tạo ra các trang web tương tác và đáp ứng đáng kể.
    Tóm lại, HTML DOM cung cấp một cách tiếp cận lập trình cho phép bạn tương tác
    với HTML và XML trên trang web của mình, làm cho trang web trở nên động và tương tác với người dùng
*/